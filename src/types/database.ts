/**
 * Shared type definitions for database operations
 */

/**
 * Type for Prisma where clause filters
 * Used in API routes for building dynamic query conditions
 */
export type WhereClause = Record<string, boolean | object | string>;
